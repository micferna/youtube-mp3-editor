fastapi>=0.138.1
uvicorn[standard]>=0.49.0
python-multipart>=0.0.32
pydantic>=2.13.4
websockets>=16.0
# yt-dlp intentionally unpinned: YouTube changes frequently and breaks older
# releases, so each image build should pull the newest version.
yt-dlp
